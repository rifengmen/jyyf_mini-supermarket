/* pages/userInfo/setting/setting.wxss */
.set_cont {
  width: calc(100% - 48rpx);
  height: auto;
  padding: 0 24rpx;
}
.set_item {
  width: 100%;
  height: 84rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.remove {
  width: calc(100% - 48rpx);
  height: calc(100vh - 278rpx);
  margin-top: 24rpx;
  padding: 0 24rpx;
}
